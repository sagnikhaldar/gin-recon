{
  "fixture": "existing-openapi-document",
  "purpose": "Exercises reconciling a pre-existing OpenAPI document (openapi.yaml) against discovered routes, per docs/adr/0013-existing-openapi-document-reconciliation.md: GetUser's route fully agrees with the document, GetOrder's route names its path parameter differently than the document (a structural conflict), PlainHandler has no document operation at all, and DELETE /legacy/{id} in the document has no matching route (an orphan).",
  "expectedRoutes": [
    {
      "method": "GET",
      "normalizedPath": "/users/:id",
      "note": "GetUser: existing-document summary/description/tags and the \"id\" parameter description are all applied; no diagnostic"
    },
    {
      "method": "GET",
      "normalizedPath": "/orders/:orderId",
      "note": "GetOrder: existing-document summary still applied (prose is never gated), but the document's \"id\" parameter name disagrees with the route's own \"orderId\" — openapi-spec-conflict, marked unrefined for parameters, no parameter description applied"
    },
    {
      "method": "GET",
      "normalizedPath": "/plain",
      "note": "PlainHandler: no corresponding document operation; route.ExistingDocument stays nil"
    }
  ],
  "expectedDiagnostics": [
    { "code": "openapi-spec-conflict", "note": "GetOrder's route parameter name \"orderId\" vs the document's \"id\"" },
    { "code": "openapi-spec-orphan-operation", "note": "DELETE /legacy/{id} is documented but not discovered in code" }
  ],
  "expectedOrphanedOperations": [
    { "method": "DELETE", "path": "/legacy/{id}", "summary": "Deprecated legacy delete operation no longer present in code" }
  ]
}
