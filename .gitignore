# Build artifacts
/bin/
/dist/
*.test
*.out

# Go
/go.work
/go.work.sum

# Coverage
coverage.txt
coverage.html

# Local scan output
/recon-out/
*.tmp-*/

# Local build output — `go build ./cmd/gin-recon` with no -o writes this
# binary straight into the repo root; never commit it.
/gin-recon

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/

# Local Claude Code settings
.claude/settings.local.json
scratchpad/

# Internal planning/comparison docs, kept locally, not published
/PLAN.md
/docs/express-parity-matrix.md
/docs/adr/
